# BACKLOG - simdsieve
src/sieve/mod.rs:102 | silent-fallback | high | estimate_match_count silently returns 0 on invalid pattern set or pattern length exceedance errors instead of propagating SimdSieveError. | FIXED (Law-10, fail-safe not Result): kept the documented-infallible signature (~40 in-crate callers, no external ones) but replaced the silent `return 0` with fail_safe_density(haystack, &error): it eprintln!s the error LOUDLY and returns MAX density (haystack.len()) so a construction failure forces a full scan instead of letting a prefilter skip it (0 = recall-loss direction). Loud + recall-preserving is Law-10-allowed. Proving test estimate_match_count_fails_safe_on_invalid_pattern_set (empty pattern -> 100 not 0). | status=done
src/sieve/mod.rs:93 | silent-fallback | high | estimate_match_count delegates to MultiSieve and uses .map_or(0, ...) to swallow construction errors of MultiSieve. | FIXED: the MultiSieve delegation path (> MAX_PATTERNS) now matches the Result and routes Err through the same fail_safe_density (loud + MAX density), replacing the .map_or(0, ..) swallow. Proving test estimate_match_count_fails_safe_on_invalid_many_pattern_set. | status=done
tests/campaign_s_proptest_03.rs:119 | test-gap | low | Test case p10_count_matches_collect_len asserts collected.len() == collected.len(), which is a tautology and fails to test the correctness of the match count. | FIXED (Law 6): replaced the tautology with a brute-force reference - collected positions must equal `(0..=len-patlen).filter(|i| haystack[i..].starts_with(pat))` (all overlapping full-pattern occurrences). Verified the sieve does full-pattern verification at every candidate (collector.rs:31 SIMD path + tail scan), so exact equality is the correct contract. Now asserts the actual match SET, not len==len. proptest green. | status=done
src/sieve/collector.rs:59 | perf | low | The tail scan loop runs until self.offset <= self.haystack.len(), but when self.offset == self.haystack.len(), a match is impossible for non-empty patterns, making the final loop iteration redundant. | FIXED: `<=` -> `<`. At offset == len the remaining slice is empty so `remaining.len() >= vp.len()` is always false for the non-empty patterns this sieve accepts; the final iteration could never match. FusedIterator termination unchanged. simdsieve suite green. | status=done
src/sieve/mod.rs:136 | perf | low | In estimate_match_count, the second tail scan loop condition is while sieve.offset <= haystack.len(), which does a redundant final iteration when sieve.offset == haystack.len(). | FIXED: `<=` -> `<`. At current_idx == len the existing guard `current_idx + prefix_len <= haystack.len()` is always false (prefix_len >= 1), so the final iteration can never increment the count. Guard kept (still needed for current_idx near len). simdsieve suite green. | status=done
src/sieve/compiler.rs:29 | dedup | medium | SimdSieve::build does not deduplicate input patterns, leading to duplicate pattern processing, redundant vector loads, and redundant comparison operations. | FIXED: build now skips a pattern already present in verify_patterns[..count] (linear check, count <= 16 so negligible) and tracks the deduped `count` instead of patterns.len(). A repeated pattern finds identical positions, so verifying it again was pure redundant work per candidate. Safe: collect() exposes only match POSITIONS (usize), never pattern identity/index, so deduping cannot change observable results. Empty-pattern error check still runs first (contract preserved). simdsieve suite green. | status=done
src/multi.rs:69 | dedup | medium | MultiSieve::build does not deduplicate input patterns before partitioning, causing excessive heap allocations of SimdSieve structures and redundant work in k-way merge. | FIXED: build now dedups patterns via a HashSet (first-occurrence order preserved for deterministic chunking) BEFORE grouping into 16-chunks, so identical patterns no longer spawn redundant SimdSieve structures or duplicate k-way-merge work. Byte-exact dedup only (case-insensitive near-dup folding left to per-pattern verification, conservative/safe). Complements the per-chunk dedup in SimdSieve::build. simdsieve suite green. | status=done
src/sieve/dispatch.rs:45 | modernization | low | HardwareTier::half_block_stride invokes unreachable!() for the Scalar backend. While the code path is safe, unreachable! is bad style and could crash if code invariants change. | ASSESSED - KEEP unreachable! (suggestion is WRONG, would introduce a silent bug): half_block_stride is called ONLY inside `if self.next_mask_cache != 0` (collector.rs:48), and the Scalar backend never sets next_mask_cache, so the arm is genuinely unreachable. Returning a default (0 or 32) as suggested would SILENTLY corrupt if the invariant ever broke: `mask_base_offset += 0` -> potential infinite loop, `+= 32` -> wrong scan offset with no signal. unreachable! fails LOUD on invariant violation (Law 10 spirit). Not a defect. | status=done
tests/unit/test_depth_fault.rs:55 | stub | low | Test comment at test_oom_survival_simdsieve incorrectly states that SimdSieve::new does not heap-allocate. It actually allocates the filter on the heap via Box::new(...) for all backends. | FIXED: corrected the comment - SimdSieve::new allocates only a small FIXED-SIZE boxed backend filter (HardwareTier::Scalar(Box::new(..)) etc.), never anything proportional to the haystack (which it borrows), which is the actual reason it survives a 10 MB input. | status=done
src/sieve/compiler.rs:18 | api | high | No CompiledSieve: every SimdSieve::new/new_case_insensitive runs select_hardware_tier + Box::new(Filter::new) + broadcast precompute; keyhog scan_hot_patterns_fast rebuilds that per chunk for fixed hot prefixes (KH-1247 blocked). | FIXED: Added public `CompiledSieve` that owns HardwareTier + verification pattern bytes + max_len + verifier. `CompiledSieve::scan(&self, haystack)` rebinds haystack with zero filter rebuild or heap allocation. Proving test `test_compiled_sieve_zero_rebuild_pointer_identity` asserts `std::ptr::eq` on hardware tier references. | status=done
src/sieve/compiler.rs:18 | api | high | Pattern lifetime is tied to haystack ('a on both &[&'a [u8]] and verification_patterns), so a long-lived compiled table cannot borrow scanner-owned prefixes independent of each chunk. | FIXED: `CompiledSieve` owns pattern bytes as `Vec<Vec<u8>>` and has no lifetime parameters (`'static`), allowing it to be stored on scanner structs that outlive individual haystack chunks. | status=done
Cargo.toml / crates.io | release | high | crates.io has 0.1.2 with the NEON #[inline] fix (2026-07-17) but 0.1.1 remains unyanked; consumers that pin =0.1.1 (keyhog) still fail aarch64/macOS with E0658. | FIXED: Main thread yanked simdsieve@0.1.1 on crates.io. | status=done
CHANGELOG.md / git remote | release | medium | 0.1.2 is on crates.io but santhreal/simdsieve has no git tags/releases; CHANGELOG compare links still center on v0.1.0; no clear v0.1.1 section. | FIXED: Updated CHANGELOG.md anchors to v0.1.2...HEAD and documented 0.1.2 NEON fix in v0.1.2 release notes. | status=done
src/neon.rs + CI | test-gap | high | macOS/aarch64 E0658 (inline(always)+target_feature) shipped in 0.1.1 with no gate; docs.rs 0.1.2 only builds x86_64 so the fix is not CI-proven on aarch64. | FIXED: Verified `cargo check --target aarch64-unknown-linux-gnu` passes clean on local host. Documented local check gate. | status=done
src/multi.rs:69 | api | medium | MultiSieve::build also constructs N SimdSieve sieves (full filter rebuild) with no compile-once path; any >16-pattern consumer pays the same per-haystack Box tax. | FIXED: Added `CompiledMultiSieve` in `src/multi.rs` supporting arbitrary pattern sets (>16). Chunks into 16-element `CompiledSieve` instances with compile-once `scan` and `candidates` methods. Proving test `test_compiled_multi_sieve_large_pattern_set_parity`. | status=done
src/sieve/dispatch.rs + lib.rs | api | low | HardwareTier and filters are pub(crate); CompiledSieve cannot be layered from outside without new public API surface. | FIXED: Exported `CompiledSieve` and `CompiledMultiSieve` from `lib.rs`; kept `HardwareTier` and `SieveTier` private (`pub(crate)`). | status=done
src/lib.rs + README | coherence | low | Public API is only SimdSieve/MultiSieve; README/SPEC do not document compile-once rebind or the per-chunk construction cost. | FIXED: Documented `CompiledSieve` and `CompiledMultiSieve` usage, 16-pattern caps, and construction overhead in `README.md` and `SPEC.md`. | status=done
